/*! gregturner.github.io 2017-11-19 */

const resumeDatabase=JSON.parse(localStorage.getItem("resume")),workElement=document.getElementById("workExp"),certElement=document.getElementById("certs"),eduElement=document.getElementById("education");for(let e in resumeDatabase){if("job"===e){let n=resumeDatabase[e];workElement.innerHTML+='\n        <div id="label">Technical Work Experience:</div>\n            <ul>\n        ';for(let e=0;e<n.length;e++){let t=n[e];workElement.innerHTML+=`\n                <li class="noStyle">${t.startDate}-${t.endDate} ${t.name}, ${t.location} [${t.title}]</li>\n                `}workElement.innerHTML+="\n        </ul>\n        "}else if("cert"===e){let n=resumeDatabase[e];certElement.innerHTML+='\n        <div id="label">Certifications:</div>\n            <ul>\n        ';for(let e=0;e<n.length;e++){let t=n[e];certElement.innerHTML+=`\n                <li class="noStyle">(${t.endDate}) ${t.name} ${t.title}</li>\n                `}certElement.innerHTML+="\n        </ul>\n        "}else{let n=resumeDatabase[e];eduElement.innerHTML+='\n        <div id="label">Education:</div>\n            <ul>\n        ';for(let e=0;e<n.length;e++){let t=n[e];eduElement.innerHTML+=`\n                <li class="noStyle">(${t.endDate}) ${t.name}, ${t.location}<br>${t.title}<br>${t.comment}</li>\n                `}}eduElement.innerHTML+="\n        </ul>\n        "}