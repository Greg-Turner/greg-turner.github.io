/*! gregturner.github.io 2017-11-19 */

const projectDatabase=JSON.parse(localStorage.getItem("projects")),projectElement=document.getElementById("project");for(let e=0;e<projectDatabase.length;e++){let t=projectDatabase[e];projectElement.innerHTML+=`\n        <br>\n        <article class="project{$i}">\n            <h2 class="label">${t.name}</h2>\n            <p class="label">Description:</p>\n            <p>${t.description}</p>\n            <p class="label">Technologies:</p>\n            <p>${t.technologies}</p>\n            <p class="label">Team:</p>\n            <p>${t.team}</p>\n            <p class="label">Competion Date: ${t.completeDate}</p>\n            \n        </article>\n        `}